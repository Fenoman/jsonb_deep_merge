-- complain if script is sourced in psql, rather than via ALTER EXTENSION
\echo Use "ALTER EXTENSION jsonb_deep_merge UPDATE TO '1.0.0'" to load this file. \quit

-- No changes in functions for version 1.0.0